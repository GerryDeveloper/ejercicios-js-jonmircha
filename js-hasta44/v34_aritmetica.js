
// export function sumar (a,b) {
function sumar (a,b) {
  return a + b;
}

function restar (a,b) {
  return a - b;
}

export const aritmetica = {
  sumar, // propiedades de objetos literales, si la propiedad se llama igual que la funcion, se pone sol una vez
  restar
}